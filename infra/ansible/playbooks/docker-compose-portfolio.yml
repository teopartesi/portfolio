---
- name: Manage Portfolio Docker Compose project
  hosts: production
  gather_facts: false

  tasks:
    - name: Ensure Portfolio Compose project is running
      community.docker.docker_compose_v2:
        project_src: "{{ portfolio_compose_dir }}"
        state: present
        pull: never
        recreate: auto
        remove_orphans: false
